NEXT STEPS

* log question together with solution string.
* Run on harder dataset and with somewhat better prompt engineering (ask Daman)
* Andrea's reward function (best guess + uncertainty score)
* logging of IDK rewards to understand why IDK is not getting picked up

QUINOTES:
log config for andrea's function. log run description, fix desc in main script

POTENTIALLY V USEFUL

* log the config parameters of each ray_actor (since they have derived )
* weave integration

WOULD BE NICE, EASY

* move all scripts to folder -- clean up generally
* {not priority becuase we want to change reward function} make idk_reward a parameter of `run_countdown` or `train_tiny_zero` (currently its just set in file) -- pretty low priority since its already logged, but we could actually have quite a few errors with the current setup
* make sure that the system prompt / reward is easily visible

WOULD BE NICE, NOT EASY AFAIK

* a new random seed each time and log it hf for reproducability
* get a pi-chart output of timining (seems that about 90% is in gen)
  * try to learn of more ways to track what GPUs are doing
* integrate with Docent.
* in OCCUSIONAL QUALITY PRINT also print out the question (currently only the answer)

EXPERIMENTS:

* show model can say idk at all.

QS TO DAMAN
Seems 7B model training works, at least while response_length is stable! About 3.5min/step -- sound reasonable?

would u suggest switching to a harder dataset so its easier to train the model to say idk?

I was thinking it might be helpful to iterate just on the system prompt to see how much it can change performance? do u have any suggestions how to do that? ideally I could very easily change the prompt to the untrained Qwen2.5-7B-Instruct and then run it on a random 20-50 question to determine the correct and idk ratios. Do you think this is worthwhile?
